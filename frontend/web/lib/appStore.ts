/**
 * Public App Store link for Offload (ASC app 6799952861).
 *
 * null until the app clears App Store review — every CTA renders as a
 * non-interactive "Coming soon" badge. Once approved, set:
 *
 *   export const APP_STORE_URL: string | null =
 *     'https://apps.apple.com/app/id6799952861';
 *
 * (A public TestFlight invite URL also works here in the interim if beta
 * signups from the site are wanted.)
 */
export const APP_STORE_URL: string | null = null;
